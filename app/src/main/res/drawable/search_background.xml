<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/white" />  <!-- White Background -->
    <corners android:radius="20dp" />  <!-- Rounded Corners -->
    <stroke android:width="1dp" android:color="@color/bottomNavIconColor" />  <!-- Border -->
    <padding android:left="16dp" android:right="16dp" />  <!-- Inner Padding -->
</shape>
